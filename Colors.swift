import SwiftUI

extension Color {
    static let accentColor = Color.pink
    static let bgColor = Color.black.opacity(1.0)
    // or
    // static let backgroundColor = Color.background // For system background
} 